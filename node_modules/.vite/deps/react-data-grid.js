import {
  require_react_dom
} from "./chunk-3JQK3TMX.js";
import {
  require_jsx_runtime
} from "./chunk-BCRBT45M.js";
import {
  require_react
} from "./chunk-BKLRCT72.js";
import {
  __toESM
} from "./chunk-G3PMV62Z.js";

// node_modules/react-data-grid/lib/bundle.js
var import_react = __toESM(require_react());

// node_modules/clsx/dist/clsx.m.js
function r(e) {
  var t, f, n = "";
  if ("string" == typeof e || "number" == typeof e) n += e;
  else if ("object" == typeof e) if (Array.isArray(e)) for (t = 0; t < e.length; t++) e[t] && (f = r(e[t])) && (n && (n += " "), n += f);
  else for (t in e) e[t] && (n && (n += " "), n += t);
  return n;
}
function clsx() {
  for (var e, t, f = 0, n = ""; f < arguments.length; ) (e = arguments[f++]) && (t = r(e)) && (n && (n += " "), n += t);
  return n;
}
var clsx_m_default = clsx;

// node_modules/react-data-grid/lib/bundle.js
var import_jsx_runtime = __toESM(require_jsx_runtime());
var import_react_dom = __toESM(require_react_dom());
function styleInject(css, ref) {
  if (ref === void 0) ref = {};
  var insertAt = ref.insertAt;
  if (!css || typeof document === "undefined") {
    return;
  }
  var head = document.head || document.getElementsByTagName("head")[0];
  var style = document.createElement("style");
  style.type = "text/css";
  if (insertAt === "top") {
    if (head.firstChild) {
      head.insertBefore(style, head.firstChild);
    } else {
      head.appendChild(style);
    }
  } else {
    head.appendChild(style);
  }
  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    style.appendChild(document.createTextNode(css));
  }
}
var css_248z$c = ".c1wupbe700-canary43{contain:strict;contain:size layout style paint;padding:0 8px;border-right:1px solid var(--border-color);border-bottom:1px solid var(--border-color);background-color:inherit;white-space:nowrap;overflow:hidden;overflow:clip;text-overflow:ellipsis}.cd0kgiy700-canary43{position:sticky;z-index:1}.c1730fa4700-canary43{box-shadow:2px 0 5px -2px hsla(0,0%,53.3%,.3)}.c9dpaye700-canary43{box-shadow:inset 0 0 0 2px var(--selection-color)}";
styleInject(css_248z$c);
var cell = "c1wupbe700-canary43";
var cellClassname = `rdg-cell ${cell}`;
var cellFrozen = "cd0kgiy700-canary43";
var cellFrozenClassname = `rdg-cell-frozen ${cellFrozen}`;
var cellFrozenLast = "c1730fa4700-canary43";
var cellFrozenLastClassname = `rdg-cell-frozen-last ${cellFrozenLast}`;
var cellSelected = "c9dpaye700-canary43";
var cellSelectedClassname = `rdg-cell-selected ${cellSelected}`;
var css_248z$b = ".r104f42s700-canary43{--color:#000;--border-color:#ddd;--summary-border-color:#aaa;--background-color:hsl(0deg 0% 100%);--header-background-color:hsl(0deg 0% 97.5%);--row-hover-background-color:hsl(0deg 0% 96%);--row-selected-background-color:hsl(207deg 76% 92%);--row-selected-hover-background-color:hsl(207deg 76% 88%);--checkbox-color:hsl(207deg 100% 29%);--checkbox-focus-color:hsl(207deg 100% 69%);--checkbox-disabled-border-color:#ccc;--checkbox-disabled-background-color:#ddd;--selection-color:#66afe9;--font-size:14px;contain:strict;contain:size layout style paint;content-visibility:auto;height:350px;border:1px solid var(--border-color);box-sizing:border-box;overflow:auto;user-select:none;background-color:var(--background-color);color:var(--color);font-size:var(--font-size)}@supports not (contain:strict){.r104f42s700-canary43{position:relative;z-index:0}}.r104f42s700-canary43 *,.r104f42s700-canary43 :after,.r104f42s700-canary43 :before{box-sizing:inherit}.r104f42s700-canary43.rdg-dark{--color:#ddd;--border-color:#444;--summary-border-color:#555;--background-color:hsl(0deg 0% 13%);--header-background-color:hsl(0deg 0% 10.5%);--row-hover-background-color:hsl(0deg 0% 9%);--row-selected-background-color:hsl(207deg 76% 42%);--row-selected-hover-background-color:hsl(207deg 76% 38%);--checkbox-color:hsl(207deg 100% 79%);--checkbox-focus-color:hsl(207deg 100% 89%);--checkbox-disabled-border-color:#000;--checkbox-disabled-background-color:#333}@media (prefers-color-scheme:dark){.r104f42s700-canary43:not(.rdg-light){--color:#ddd;--border-color:#444;--summary-border-color:#555;--background-color:hsl(0deg 0% 13%);--header-background-color:hsl(0deg 0% 10.5%);--row-hover-background-color:hsl(0deg 0% 9%);--row-selected-background-color:hsl(207deg 76% 42%);--row-selected-hover-background-color:hsl(207deg 76% 38%);--checkbox-color:hsl(207deg 100% 79%);--checkbox-focus-color:hsl(207deg 100% 89%);--checkbox-disabled-border-color:#000;--checkbox-disabled-background-color:#333}}.f7ly7s700-canary43{position:sticky;top:0;left:0;height:0;width:0;outline:0}.vc4f4zb700-canary43.r1otpg64700-canary43{cursor:move}";
styleInject(css_248z$b);
var root = "r104f42s700-canary43";
var rootClassname = `rdg ${root}`;
var focusSink = "f7ly7s700-canary43";
var focusSinkClassname = `rdg-focus-sink ${focusSink}`;
var viewportDragging = "vc4f4zb700-canary43";
var viewportDraggingClassname = `rdg-viewport-dragging ${viewportDragging}`;
var css_248z$a = '.g1cvx5us700-canary43:not(.rel5gk2700-canary43){background-color:var(--header-background-color)}.g1cvx5us700-canary43>.c1wupbe700-canary43:not(:last-child):not(.c1730fa4700-canary43){border-right:none}.g1j2w62i700-canary43:after{content:"";position:absolute;top:0;right:0;bottom:0;left:0;box-shadow:inset 0 0 0 2px var(--selection-color);pointer-events:none;z-index:2}.g1j2w62i700-canary43>.c1wupbe700-canary43:first-child{box-shadow:inset 2px 0 0 0 var(--selection-color)}';
styleInject(css_248z$a);
var groupRow = "g1cvx5us700-canary43";
var groupRowClassname = `rdg-group-row ${groupRow}`;
var groupRowSelected = "g1j2w62i700-canary43";
var groupRowSelectedClassname = `rdg-group-row-selected ${groupRowSelected}`;
var css_248z$9 = ".h1fquj5h700-canary43{contain:strict;contain:size layout style paint;display:grid;grid-template-columns:var(--template-columns);width:var(--row-width);position:sticky;background-color:var(--header-background-color);font-weight:700;z-index:3}.h19m9p7h700-canary43{grid-template-rows:var(--header-row-height);height:var(--header-row-height);line-height:var(--header-row-height);top:0;touch-action:none}.fkdzs1h700-canary43{grid-template-rows:var(--filter-row-height);height:var(--filter-row-height);line-height:var(--filter-row-height);top:var(--header-row-height)}";
styleInject(css_248z$9);
var headerRowAndFilterRow = "h1fquj5h700-canary43";
var headerRow = "h19m9p7h700-canary43";
var headerRowClassname = `rdg-header-row ${headerRowAndFilterRow} ${headerRow}`;
var filterRow = "fkdzs1h700-canary43";
var filterRowClassname = `rdg-filter-row ${headerRowAndFilterRow} ${filterRow}`;
var css_248z$8 = ".r1otpg64700-canary43{contain:strict;contain:size layout style paint;display:grid;grid-template-rows:var(--row-height);grid-template-columns:var(--template-columns);position:absolute;left:0;width:var(--row-width);height:var(--row-height);line-height:var(--row-height);background-color:var(--background-color)}.r1otpg64700-canary43:hover{background-color:var(--row-hover-background-color)}.rel5gk2700-canary43{background-color:var(--row-selected-background-color)}.rel5gk2700-canary43:hover{background-color:var(--row-selected-hover-background-color)}.s1qymf1z700-canary43{position:sticky;z-index:3;grid-template-rows:var(--summary-row-height);height:var(--summary-row-height);line-height:var(--summary-row-height)}.s1qymf1z700-canary43>.c1wupbe700-canary43{border-top:2px solid var(--summary-border-color)}";
styleInject(css_248z$8);
var row = "r1otpg64700-canary43";
var rowClassname = `rdg-row ${row}`;
var rowSelected = "rel5gk2700-canary43";
var rowSelectedClassname = `rdg-row-selected ${rowSelected}`;
var summaryRow = "s1qymf1z700-canary43";
var summaryRowClassname = `rdg-summary-row ${summaryRow}`;
function useFocusRef(isCellSelected) {
  const ref = (0, import_react.useRef)(null);
  (0, import_react.useLayoutEffect)(() => {
    var _ref$current;
    if (!isCellSelected) return;
    (_ref$current = ref.current) == null ? void 0 : _ref$current.focus({
      preventScroll: true
    });
  }, [isCellSelected]);
  return ref;
}
var css_248z$7 = '.c1w6d5eo700-canary43{cursor:pointer;display:flex;align-items:center;justify-content:center;position:absolute;top:0;right:0;bottom:0;left:0;margin-right:1px}.c1h7iz8d700-canary43{all:unset;width:0;margin:0}.cc79ydj700-canary43{content:"";width:20px;height:20px;border:2px solid var(--border-color);background-color:var(--background-color)}.c1h7iz8d700-canary43:checked+.cc79ydj700-canary43{background-color:var(--checkbox-color);box-shadow:inset 0 0 0 4px var(--background-color)}.c1h7iz8d700-canary43:focus+.cc79ydj700-canary43{border-color:var(--checkbox-focus-color)}.c1e5jt0b700-canary43{cursor:default}.c1e5jt0b700-canary43 .cc79ydj700-canary43{border-color:var(--checkbox-disabled-border-color);background-color:var(--checkbox-disabled-background-color)}';
styleInject(css_248z$7);
var checkboxLabel = "c1w6d5eo700-canary43";
var checkboxLabelClassname = `rdg-checkbox-label ${checkboxLabel}`;
var checkboxInput = "c1h7iz8d700-canary43";
var checkboxInputClassname = `rdg-checkbox-input ${checkboxInput}`;
var checkbox = "cc79ydj700-canary43";
var checkboxClassname = `rdg-checkbox ${checkbox}`;
var checkboxLabelDisabled = "c1e5jt0b700-canary43";
var checkboxLabelDisabledClassname = `rdg-checkbox-label-disabled ${checkboxLabelDisabled}`;
function SelectCellFormatter({
  value,
  tabIndex,
  isCellSelected,
  disabled,
  onClick,
  onChange,
  "aria-label": ariaLabel,
  "aria-labelledby": ariaLabelledBy
}) {
  const inputRef = useFocusRef(isCellSelected);
  function handleChange(e) {
    onChange(e.target.checked, e.nativeEvent.shiftKey);
  }
  return (0, import_jsx_runtime.jsxs)("label", {
    className: clsx_m_default(checkboxLabelClassname, disabled && checkboxLabelDisabledClassname),
    children: [(0, import_jsx_runtime.jsx)("input", {
      "aria-label": ariaLabel,
      "aria-labelledby": ariaLabelledBy,
      tabIndex,
      ref: inputRef,
      type: "checkbox",
      className: checkboxInputClassname,
      disabled,
      checked: value,
      onChange: handleChange,
      onClick
    }), (0, import_jsx_runtime.jsx)("div", {
      className: checkboxClassname
    })]
  });
}
function ValueFormatter(props) {
  try {
    return (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, {
      children: props.row[props.column.key]
    });
  } catch {
    return null;
  }
}
var css_248z$6 = ".gch972y700-canary43{outline:none}.cz2qf0d700-canary43{margin-left:4px;stroke:currentColor;stroke-width:1.5px;fill:transparent;vertical-align:middle}.cz2qf0d700-canary43>path{transition:d .1s}";
styleInject(css_248z$6);
var groupCellContent = "gch972y700-canary43";
var groupCellContentClassname = `rdg-group-cell-content ${groupCellContent}`;
var caret = "cz2qf0d700-canary43";
var caretClassname = `rdg-caret ${caret}`;
function ToggleGroupFormatter({
  groupKey,
  isExpanded,
  isCellSelected,
  toggleGroup
}) {
  const cellRef = useFocusRef(isCellSelected);
  function handleKeyDown({
    key
  }) {
    if (key === "Enter") {
      toggleGroup();
    }
  }
  const d = isExpanded ? "M1 1 L 7 7 L 13 1" : "M1 7 L 7 1 L 13 7";
  return (0, import_jsx_runtime.jsxs)("span", {
    ref: cellRef,
    className: groupCellContentClassname,
    tabIndex: -1,
    onKeyDown: handleKeyDown,
    children: [groupKey, (0, import_jsx_runtime.jsx)("svg", {
      viewBox: "0 0 14 8",
      width: "14",
      height: "8",
      className: caretClassname,
      children: (0, import_jsx_runtime.jsx)("path", {
        d
      })
    })]
  });
}
function stopPropagation(event) {
  event.stopPropagation();
}
var SELECT_COLUMN_KEY = "select-row";
var SelectColumn = {
  key: SELECT_COLUMN_KEY,
  name: "",
  width: 35,
  maxWidth: 35,
  resizable: false,
  sortable: false,
  frozen: true,
  headerRenderer(props) {
    return (0, import_jsx_runtime.jsx)(SelectCellFormatter, {
      "aria-label": "Select All",
      value: props.allRowsSelected,
      onChange: props.onAllRowsSelectionChange
    });
  },
  formatter(props) {
    return (0, import_jsx_runtime.jsx)(SelectCellFormatter, {
      "aria-label": "Select",
      tabIndex: -1,
      isCellSelected: props.isCellSelected,
      value: props.isRowSelected,
      onClick: stopPropagation,
      onChange: props.onRowSelectionChange
    });
  },
  groupFormatter(props) {
    return (0, import_jsx_runtime.jsx)(SelectCellFormatter, {
      "aria-label": "Select Group",
      tabIndex: -1,
      isCellSelected: props.isCellSelected,
      value: props.isRowSelected,
      onChange: props.onRowSelectionChange,
      onClick: stopPropagation
    });
  }
};
function useCalculatedColumns({
  rawColumns,
  columnWidths,
  viewportWidth,
  scrollLeft,
  defaultColumnOptions,
  rawGroupBy,
  enableVirtualization
}) {
  var _defaultColumnOptions, _defaultColumnOptions2, _defaultColumnOptions3, _defaultColumnOptions4;
  const minColumnWidth = (_defaultColumnOptions = defaultColumnOptions == null ? void 0 : defaultColumnOptions.minWidth) != null ? _defaultColumnOptions : 80;
  const defaultFormatter = (_defaultColumnOptions2 = defaultColumnOptions == null ? void 0 : defaultColumnOptions.formatter) != null ? _defaultColumnOptions2 : ValueFormatter;
  const defaultSortable = (_defaultColumnOptions3 = defaultColumnOptions == null ? void 0 : defaultColumnOptions.sortable) != null ? _defaultColumnOptions3 : false;
  const defaultResizable = (_defaultColumnOptions4 = defaultColumnOptions == null ? void 0 : defaultColumnOptions.resizable) != null ? _defaultColumnOptions4 : false;
  const {
    columns,
    colSpanColumns,
    lastFrozenColumnIndex,
    groupBy
  } = (0, import_react.useMemo)(() => {
    const groupBy2 = [];
    let lastFrozenColumnIndex2 = -1;
    const columns2 = rawColumns.map((rawColumn) => {
      var _rawGroupBy$includes, _rawColumn$sortable, _rawColumn$resizable, _rawColumn$formatter;
      const rowGroup = (_rawGroupBy$includes = rawGroupBy == null ? void 0 : rawGroupBy.includes(rawColumn.key)) != null ? _rawGroupBy$includes : false;
      const frozen = rowGroup || rawColumn.frozen || false;
      const column = {
        ...rawColumn,
        idx: 0,
        frozen,
        isLastFrozenColumn: false,
        rowGroup,
        sortable: (_rawColumn$sortable = rawColumn.sortable) != null ? _rawColumn$sortable : defaultSortable,
        resizable: (_rawColumn$resizable = rawColumn.resizable) != null ? _rawColumn$resizable : defaultResizable,
        formatter: (_rawColumn$formatter = rawColumn.formatter) != null ? _rawColumn$formatter : defaultFormatter
      };
      if (rowGroup) {
        var _column$groupFormatte;
        (_column$groupFormatte = column.groupFormatter) != null ? _column$groupFormatte : column.groupFormatter = ToggleGroupFormatter;
      }
      if (frozen) {
        lastFrozenColumnIndex2++;
      }
      return column;
    });
    columns2.sort(({
      key: aKey,
      frozen: frozenA
    }, {
      key: bKey,
      frozen: frozenB
    }) => {
      if (aKey === SELECT_COLUMN_KEY) return -1;
      if (bKey === SELECT_COLUMN_KEY) return 1;
      if (rawGroupBy != null && rawGroupBy.includes(aKey)) {
        if (rawGroupBy.includes(bKey)) {
          return rawGroupBy.indexOf(aKey) - rawGroupBy.indexOf(bKey);
        }
        return -1;
      }
      if (rawGroupBy != null && rawGroupBy.includes(bKey)) return 1;
      if (frozenA) {
        if (frozenB) return 0;
        return -1;
      }
      if (frozenB) return 1;
      return 0;
    });
    const colSpanColumns2 = [];
    columns2.forEach((column, idx) => {
      column.idx = idx;
      if (column.rowGroup) {
        groupBy2.push(column.key);
      }
      if (column.colSpan !== void 0) {
        colSpanColumns2.push(column);
      }
    });
    if (lastFrozenColumnIndex2 !== -1) {
      columns2[lastFrozenColumnIndex2].isLastFrozenColumn = true;
    }
    return {
      columns: columns2,
      colSpanColumns: colSpanColumns2,
      lastFrozenColumnIndex: lastFrozenColumnIndex2,
      groupBy: groupBy2
    };
  }, [rawColumns, defaultFormatter, defaultResizable, defaultSortable, rawGroupBy]);
  const {
    layoutCssVars,
    totalColumnWidth,
    totalFrozenColumnWidth,
    columnMetrics
  } = (0, import_react.useMemo)(() => {
    const columnMetrics2 = /* @__PURE__ */ new Map();
    let left = 0;
    let totalColumnWidth2 = 0;
    let totalFrozenColumnWidth2 = 0;
    let templateColumns = "";
    let allocatedWidth = 0;
    let unassignedColumnsCount = 0;
    for (const column of columns) {
      let width = getSpecifiedWidth(column, columnWidths, viewportWidth);
      if (width === void 0) {
        unassignedColumnsCount++;
      } else {
        width = clampColumnWidth(width, column, minColumnWidth);
        allocatedWidth += width;
        columnMetrics2.set(column, {
          width,
          left: 0
        });
      }
    }
    const unallocatedWidth = viewportWidth - allocatedWidth;
    const unallocatedColumnWidth = unallocatedWidth / unassignedColumnsCount;
    for (const column of columns) {
      let width;
      if (columnMetrics2.has(column)) {
        const columnMetric = columnMetrics2.get(column);
        columnMetric.left = left;
        ({
          width
        } = columnMetric);
      } else {
        width = clampColumnWidth(unallocatedColumnWidth, column, minColumnWidth);
        columnMetrics2.set(column, {
          width,
          left
        });
      }
      totalColumnWidth2 += width;
      left += width;
      templateColumns += `${width}px `;
    }
    if (lastFrozenColumnIndex !== -1) {
      const columnMetric = columnMetrics2.get(columns[lastFrozenColumnIndex]);
      totalFrozenColumnWidth2 = columnMetric.left + columnMetric.width;
    }
    const layoutCssVars2 = {
      "--template-columns": templateColumns
    };
    for (let i = 0; i <= lastFrozenColumnIndex; i++) {
      const column = columns[i];
      layoutCssVars2[`--frozen-left-${column.key}`] = `${columnMetrics2.get(column).left}px`;
    }
    return {
      layoutCssVars: layoutCssVars2,
      totalColumnWidth: totalColumnWidth2,
      totalFrozenColumnWidth: totalFrozenColumnWidth2,
      columnMetrics: columnMetrics2
    };
  }, [columnWidths, columns, viewportWidth, minColumnWidth, lastFrozenColumnIndex]);
  const [colOverscanStartIdx, colOverscanEndIdx] = (0, import_react.useMemo)(() => {
    if (!enableVirtualization) {
      return [0, columns.length - 1];
    }
    const viewportLeft = scrollLeft + totalFrozenColumnWidth;
    const viewportRight = scrollLeft + viewportWidth;
    const lastColIdx = columns.length - 1;
    const firstUnfrozenColumnIdx = Math.min(lastFrozenColumnIndex + 1, lastColIdx);
    if (viewportLeft >= viewportRight) {
      return [firstUnfrozenColumnIdx, firstUnfrozenColumnIdx];
    }
    let colVisibleStartIdx = firstUnfrozenColumnIdx;
    while (colVisibleStartIdx < lastColIdx) {
      const {
        left,
        width
      } = columnMetrics.get(columns[colVisibleStartIdx]);
      if (left + width > viewportLeft) {
        break;
      }
      colVisibleStartIdx++;
    }
    let colVisibleEndIdx = colVisibleStartIdx;
    while (colVisibleEndIdx < lastColIdx) {
      const {
        left,
        width
      } = columnMetrics.get(columns[colVisibleEndIdx]);
      if (left + width >= viewportRight) {
        break;
      }
      colVisibleEndIdx++;
    }
    const colOverscanStartIdx2 = Math.max(firstUnfrozenColumnIdx, colVisibleStartIdx - 1);
    const colOverscanEndIdx2 = Math.min(lastColIdx, colVisibleEndIdx + 1);
    return [colOverscanStartIdx2, colOverscanEndIdx2];
  }, [columnMetrics, columns, lastFrozenColumnIndex, scrollLeft, totalFrozenColumnWidth, viewportWidth, enableVirtualization]);
  return {
    columns,
    colSpanColumns,
    colOverscanStartIdx,
    colOverscanEndIdx,
    layoutCssVars,
    columnMetrics,
    totalColumnWidth,
    lastFrozenColumnIndex,
    totalFrozenColumnWidth,
    groupBy
  };
}
function getSpecifiedWidth({
  key,
  width
}, columnWidths, viewportWidth) {
  if (columnWidths.has(key)) {
    return columnWidths.get(key);
  }
  if (typeof width === "number") {
    return width;
  }
  if (typeof width === "string" && /^\d+%$/.test(width)) {
    return Math.floor(viewportWidth * parseInt(width, 10) / 100);
  }
  return void 0;
}
function clampColumnWidth(width, {
  minWidth,
  maxWidth
}, minColumnWidth) {
  width = Math.max(width, minWidth != null ? minWidth : minColumnWidth);
  if (typeof maxWidth === "number") {
    return Math.min(width, maxWidth);
  }
  return width;
}
function useClickOutside(onClick) {
  const frameRequestRef = (0, import_react.useRef)();
  function cancelAnimationFrameRequest() {
    if (typeof frameRequestRef.current === "number") {
      cancelAnimationFrame(frameRequestRef.current);
      frameRequestRef.current = void 0;
    }
  }
  const onClickRef = (0, import_react.useRef)(() => {
    throw new Error("Cannot call an event handler while rendering.");
  });
  (0, import_react.useEffect)(() => {
    onClickRef.current = onClick;
  });
  (0, import_react.useEffect)(() => {
    function onOutsideClick() {
      frameRequestRef.current = void 0;
      onClickRef.current();
    }
    function onWindowCaptureClick() {
      cancelAnimationFrameRequest();
      frameRequestRef.current = requestAnimationFrame(onOutsideClick);
    }
    window.addEventListener("click", onWindowCaptureClick, {
      capture: true
    });
    return () => {
      window.removeEventListener("click", onWindowCaptureClick, {
        capture: true
      });
      cancelAnimationFrameRequest();
    };
  }, []);
  return cancelAnimationFrameRequest;
}
function useGridDimensions() {
  const gridRef = (0, import_react.useRef)(null);
  const [gridWidth, setGridWidth] = (0, import_react.useState)(1);
  const [gridHeight, setGridHeight] = (0, import_react.useState)(1);
  (0, import_react.useLayoutEffect)(() => {
    const {
      ResizeObserver
    } = window;
    if (ResizeObserver == null) return;
    const resizeObserver = new ResizeObserver(() => {
      const {
        clientWidth,
        clientHeight
      } = gridRef.current;
      setGridWidth(clientWidth);
      setGridHeight(clientHeight);
    });
    resizeObserver.observe(gridRef.current);
    return () => {
      resizeObserver.disconnect();
    };
  }, []);
  return [gridRef, gridWidth, gridHeight];
}
function getColSpan(column, lastFrozenColumnIndex, args) {
  const colSpan = typeof column.colSpan === "function" ? column.colSpan(args) : 1;
  if (Number.isInteger(colSpan) && colSpan > 1 && (!column.frozen || column.idx + colSpan - 1 <= lastFrozenColumnIndex)) {
    return colSpan;
  }
  return void 0;
}
var nonInputKeys = /* @__PURE__ */ new Set(["Unidentified", "Alt", "AltGraph", "CapsLock", "Control", "Fn", "FnLock", "Meta", "NumLock", "ScrollLock", "Shift", "Tab", "ArrowDown", "ArrowLeft", "ArrowRight", "ArrowUp", "End", "Home", "PageDown", "PageUp", "Insert", "ContextMenu", "Escape", "Pause", "Play", "PrintScreen", "F1", "F3", "F4", "F5", "F6", "F7", "F8", "F9", "F10", "F11", "F12"]);
function isCtrlKeyHeldDown(e) {
  return (e.ctrlKey || e.metaKey) && e.key !== "Control";
}
function isDefaultCellInput(event) {
  return !nonInputKeys.has(event.key);
}
function onEditorNavigation({
  key,
  target
}) {
  if (key === "Tab" && (target instanceof HTMLInputElement || target instanceof HTMLTextAreaElement || target instanceof HTMLSelectElement)) {
    return target.matches(".rdg-editor-container > :only-child, .rdg-editor-container > label:only-child > :only-child");
  }
  return false;
}
function isSelectedCellEditable({
  selectedPosition,
  columns,
  rows,
  isGroupRow
}) {
  const column = columns[selectedPosition.idx];
  const row2 = rows[selectedPosition.rowIdx];
  return !isGroupRow(row2) && isCellEditable(column, row2);
}
function isCellEditable(column, row2) {
  return column.editor != null && !column.rowGroup && (typeof column.editable === "function" ? column.editable(row2) : column.editable) !== false;
}
function getNextSelectedCellPosition({
  cellNavigationMode,
  columns,
  colSpanColumns,
  rows,
  currentPosition,
  nextPosition,
  lastFrozenColumnIndex,
  isCellWithinBounds,
  isGroupRow
}) {
  const rowsCount = rows.length;
  let position = nextPosition;
  const setColSpan = (moveRight) => {
    const row2 = rows[position.rowIdx];
    if (!isGroupRow(row2)) {
      const posIdx = position.idx;
      for (const column of colSpanColumns) {
        const colIdx = column.idx;
        if (colIdx > posIdx) break;
        const colSpan = getColSpan(column, lastFrozenColumnIndex, {
          type: "ROW",
          row: row2
        });
        if (colSpan && posIdx > colIdx && posIdx < colSpan + colIdx) {
          position.idx = colIdx + (moveRight ? colSpan : 0);
          break;
        }
      }
    }
  };
  if (isCellWithinBounds(position)) {
    setColSpan(position.idx - currentPosition.idx > 0);
  }
  if (cellNavigationMode !== "NONE") {
    const {
      idx,
      rowIdx
    } = nextPosition;
    const columnsCount = columns.length;
    const isAfterLastColumn = idx === columnsCount;
    const isBeforeFirstColumn = idx === -1;
    if (isAfterLastColumn) {
      if (cellNavigationMode === "CHANGE_ROW") {
        const isLastRow = rowIdx === rowsCount - 1;
        if (!isLastRow) {
          position = {
            idx: 0,
            rowIdx: rowIdx + 1
          };
        }
      } else {
        position = {
          rowIdx,
          idx: 0
        };
      }
      setColSpan(true);
    } else if (isBeforeFirstColumn) {
      if (cellNavigationMode === "CHANGE_ROW") {
        const isFirstRow = rowIdx === 0;
        if (!isFirstRow) {
          position = {
            rowIdx: rowIdx - 1,
            idx: columnsCount - 1
          };
        }
      } else {
        position = {
          rowIdx,
          idx: columnsCount - 1
        };
      }
    }
    setColSpan(false);
  }
  return position;
}
function canExitGrid({
  cellNavigationMode,
  columns,
  rowsCount,
  selectedPosition: {
    rowIdx,
    idx
  },
  shiftKey
}) {
  if (cellNavigationMode === "NONE" || cellNavigationMode === "CHANGE_ROW") {
    const atLastCellInRow = idx === columns.length - 1;
    const atFirstCellInRow = idx === 0;
    const atLastRow = rowIdx === rowsCount - 1;
    const atFirstRow = rowIdx === 0;
    return shiftKey ? atFirstCellInRow && atFirstRow : atLastCellInRow && atLastRow;
  }
  return false;
}
function assertIsValidKeyGetter(keyGetter) {
  if (typeof keyGetter !== "function") {
    throw new Error("Please specify the rowKeyGetter prop to use selection");
  }
}
function getCellStyle(column, colSpan) {
  return {
    gridColumnStart: column.idx + 1,
    gridColumnEnd: colSpan !== void 0 ? `span ${colSpan}` : void 0,
    left: column.frozen ? `var(--frozen-left-${column.key})` : void 0
  };
}
function getCellClassname(column, ...extraClasses) {
  return clsx_m_default(cellClassname, ...extraClasses, column.frozen && cellFrozenClassname, column.isLastFrozenColumn && cellFrozenLastClassname);
}
function useViewportColumns({
  columns,
  colSpanColumns,
  rows,
  summaryRows,
  colOverscanStartIdx,
  colOverscanEndIdx,
  lastFrozenColumnIndex,
  rowOverscanStartIdx,
  rowOverscanEndIdx,
  enableFilterRow,
  isGroupRow
}) {
  const startIdx = (0, import_react.useMemo)(() => {
    if (colOverscanStartIdx === 0) return 0;
    let startIdx2 = colOverscanStartIdx;
    const updateStartIdx = (colIdx, colSpan) => {
      if (colSpan !== void 0 && colIdx + colSpan > colOverscanStartIdx) {
        startIdx2 = colIdx;
        return true;
      }
      return false;
    };
    for (const column of colSpanColumns) {
      const colIdx = column.idx;
      if (colIdx >= startIdx2) break;
      if (updateStartIdx(colIdx, getColSpan(column, lastFrozenColumnIndex, {
        type: "HEADER"
      }))) break;
      if (enableFilterRow && updateStartIdx(colIdx, getColSpan(column, lastFrozenColumnIndex, {
        type: "FILTER"
      }))) break;
      for (let rowIdx = rowOverscanStartIdx; rowIdx <= rowOverscanEndIdx; rowIdx++) {
        const row2 = rows[rowIdx];
        if (isGroupRow(row2)) continue;
        if (updateStartIdx(colIdx, getColSpan(column, lastFrozenColumnIndex, {
          type: "ROW",
          row: row2
        }))) break;
      }
      if (summaryRows !== void 0) {
        for (const row2 of summaryRows) {
          if (updateStartIdx(colIdx, getColSpan(column, lastFrozenColumnIndex, {
            type: "SUMMARY",
            row: row2
          }))) break;
        }
      }
    }
    return startIdx2;
  }, [rowOverscanStartIdx, rowOverscanEndIdx, rows, summaryRows, colOverscanStartIdx, lastFrozenColumnIndex, colSpanColumns, isGroupRow, enableFilterRow]);
  return (0, import_react.useMemo)(() => {
    const viewportColumns = [];
    for (let colIdx = 0; colIdx <= colOverscanEndIdx; colIdx++) {
      const column = columns[colIdx];
      if (colIdx < startIdx && !column.frozen) continue;
      viewportColumns.push(column);
    }
    return viewportColumns;
  }, [startIdx, colOverscanEndIdx, columns]);
}
var RENDER_BACTCH_SIZE = 8;
function isReadonlyArray(arr) {
  return Array.isArray(arr);
}
function useViewportRows({
  rawRows,
  rowHeight,
  clientHeight,
  scrollTop,
  groupBy,
  rowGrouper,
  expandedGroupIds,
  enableVirtualization
}) {
  const [groupedRows, rowsCount] = (0, import_react.useMemo)(() => {
    if (groupBy.length === 0 || !rowGrouper) return [void 0, rawRows.length];
    const groupRows = (rows2, [groupByKey, ...remainingGroupByKeys], startRowIndex) => {
      let groupRowsCount = 0;
      const groups = {};
      for (const [key, childRows] of Object.entries(rowGrouper(rows2, groupByKey))) {
        const [childGroups, childRowsCount] = remainingGroupByKeys.length === 0 ? [childRows, childRows.length] : groupRows(childRows, remainingGroupByKeys, startRowIndex + groupRowsCount + 1);
        groups[key] = {
          childRows,
          childGroups,
          startRowIndex: startRowIndex + groupRowsCount
        };
        groupRowsCount += childRowsCount + 1;
      }
      return [groups, groupRowsCount];
    };
    return groupRows(rawRows, groupBy, 0);
  }, [groupBy, rowGrouper, rawRows]);
  const [rows, isGroupRow] = (0, import_react.useMemo)(() => {
    const allGroupRows = /* @__PURE__ */ new Set();
    if (!groupedRows) return [rawRows, isGroupRow2];
    const flattenedRows = [];
    const expandGroup = (rows2, parentId, level) => {
      if (isReadonlyArray(rows2)) {
        flattenedRows.push(...rows2);
        return;
      }
      Object.keys(rows2).forEach((groupKey, posInSet, keys) => {
        var _expandedGroupIds$has;
        const id = parentId !== void 0 ? `${parentId}__${groupKey}` : groupKey;
        const isExpanded = (_expandedGroupIds$has = expandedGroupIds == null ? void 0 : expandedGroupIds.has(id)) != null ? _expandedGroupIds$has : false;
        const {
          childRows,
          childGroups,
          startRowIndex
        } = rows2[groupKey];
        const groupRow2 = {
          id,
          parentId,
          groupKey,
          isExpanded,
          childRows,
          level,
          posInSet,
          startRowIndex,
          setSize: keys.length
        };
        flattenedRows.push(groupRow2);
        allGroupRows.add(groupRow2);
        if (isExpanded) {
          expandGroup(childGroups, id, level + 1);
        }
      });
    };
    expandGroup(groupedRows, void 0, 0);
    return [flattenedRows, isGroupRow2];
    function isGroupRow2(row2) {
      return allGroupRows.has(row2);
    }
  }, [expandedGroupIds, groupedRows, rawRows]);
  if (!enableVirtualization) {
    return {
      rowOverscanStartIdx: 0,
      rowOverscanEndIdx: rows.length - 1,
      rows,
      rowsCount,
      isGroupRow
    };
  }
  const overscanThreshold = 4;
  const rowVisibleStartIdx = Math.floor(scrollTop / rowHeight);
  const rowVisibleEndIdx = Math.min(rows.length - 1, Math.floor((scrollTop + clientHeight) / rowHeight));
  const rowOverscanStartIdx = Math.max(0, Math.floor((rowVisibleStartIdx - overscanThreshold) / RENDER_BACTCH_SIZE) * RENDER_BACTCH_SIZE);
  const rowOverscanEndIdx = Math.min(rows.length - 1, Math.ceil((rowVisibleEndIdx + overscanThreshold) / RENDER_BACTCH_SIZE) * RENDER_BACTCH_SIZE);
  return {
    rowOverscanStartIdx,
    rowOverscanEndIdx,
    rows,
    rowsCount,
    isGroupRow
  };
}
function useLatestFunc(fn) {
  const ref = (0, import_react.useRef)(fn);
  (0, import_react.useEffect)(() => {
    ref.current = fn;
  });
  return (0, import_react.useCallback)((...args) => {
    ref.current(...args);
  }, []);
}
var css_248z$5 = ".h13yq3r8700-canary43{cursor:pointer;display:flex}.ht6rdyl700-canary43{flex-grow:1;overflow:hidden;overflow:clip;text-overflow:ellipsis}";
styleInject(css_248z$5);
var headerSortCell = "h13yq3r8700-canary43";
var headerSortCellClassname = `rdg-header-sort-cell ${headerSortCell}`;
var headerSortName = "ht6rdyl700-canary43";
var headerSortNameClassname = `rdg-header-sort-name ${headerSortName}`;
function SortableHeaderCell({
  column,
  onSort,
  sortColumn,
  sortDirection,
  children
}) {
  sortDirection = sortColumn === column.key && sortDirection || "NONE";
  let sortText = "";
  if (sortDirection === "ASC") {
    sortText = "▲";
  } else if (sortDirection === "DESC") {
    sortText = "▼";
  }
  function onClick() {
    if (!onSort) return;
    const {
      sortDescendingFirst
    } = column;
    let direction;
    switch (sortDirection) {
      case "ASC":
        direction = sortDescendingFirst ? "NONE" : "DESC";
        break;
      case "DESC":
        direction = sortDescendingFirst ? "ASC" : "NONE";
        break;
      default:
        direction = sortDescendingFirst ? "DESC" : "ASC";
        break;
    }
    onSort(column.key, direction);
  }
  return (0, import_jsx_runtime.jsxs)("span", {
    className: headerSortCellClassname,
    onClick,
    children: [(0, import_jsx_runtime.jsx)("span", {
      className: headerSortNameClassname,
      children
    }), (0, import_jsx_runtime.jsx)("span", {
      children: sortText
    })]
  });
}
var css_248z$4 = '.celq7o9700-canary43:after{content:"";cursor:col-resize;position:absolute;top:0;right:0;bottom:0;width:10px}';
styleInject(css_248z$4);
var cellResizable = "celq7o9700-canary43";
var cellResizableClassname = `rdg-cell-resizable ${cellResizable}`;
function getAriaSort(sortDirection) {
  switch (sortDirection) {
    case "ASC":
      return "ascending";
    case "DESC":
      return "descending";
    default:
      return "none";
  }
}
function HeaderCell({
  column,
  colSpan,
  onResize,
  allRowsSelected,
  onAllRowsSelectionChange,
  sortColumn,
  sortDirection,
  onSort
}) {
  function onPointerDown(event) {
    if (event.pointerType === "mouse" && event.buttons !== 1) {
      return;
    }
    const {
      currentTarget,
      pointerId
    } = event;
    const {
      right
    } = currentTarget.getBoundingClientRect();
    const offset = right - event.clientX;
    if (offset > 11) {
      return;
    }
    function onPointerMove(event2) {
      if (event2.pointerId !== pointerId) return;
      if (event2.pointerType === "mouse" && event2.buttons !== 1) {
        onPointerUp(event2);
        return;
      }
      const width = event2.clientX + offset - currentTarget.getBoundingClientRect().left;
      if (width > 0) {
        onResize(column, width);
      }
    }
    function onPointerUp(event2) {
      if (event2.pointerId !== pointerId) return;
      window.removeEventListener("pointermove", onPointerMove);
      window.removeEventListener("pointerup", onPointerUp);
    }
    event.preventDefault();
    window.addEventListener("pointermove", onPointerMove);
    window.addEventListener("pointerup", onPointerUp);
  }
  function getCell() {
    if (column.headerRenderer) {
      return (0, import_jsx_runtime.jsx)(column.headerRenderer, {
        column,
        sortColumn,
        sortDirection,
        onSort,
        allRowsSelected,
        onAllRowsSelectionChange
      });
    }
    if (column.sortable) {
      return (0, import_jsx_runtime.jsx)(SortableHeaderCell, {
        column,
        onSort,
        sortColumn,
        sortDirection,
        children: column.name
      });
    }
    return column.name;
  }
  const className = getCellClassname(column, column.headerCellClass, column.resizable && cellResizableClassname);
  return (0, import_jsx_runtime.jsx)("div", {
    role: "columnheader",
    "aria-colindex": column.idx + 1,
    "aria-sort": sortColumn === column.key ? getAriaSort(sortDirection) : void 0,
    "aria-colspan": colSpan,
    className,
    style: getCellStyle(column, colSpan),
    onPointerDown: column.resizable ? onPointerDown : void 0,
    children: getCell()
  });
}
function HeaderRow({
  columns,
  rows,
  rowKeyGetter,
  onSelectedRowsChange,
  allRowsSelected,
  onColumnResize,
  sortColumn,
  sortDirection,
  onSort,
  lastFrozenColumnIndex
}) {
  const handleAllRowsSelectionChange = (0, import_react.useCallback)((checked) => {
    if (!onSelectedRowsChange) return;
    assertIsValidKeyGetter(rowKeyGetter);
    const newSelectedRows = new Set(checked ? rows.map(rowKeyGetter) : void 0);
    onSelectedRowsChange(newSelectedRows);
  }, [onSelectedRowsChange, rows, rowKeyGetter]);
  const cells = [];
  for (let index = 0; index < columns.length; index++) {
    const column = columns[index];
    const colSpan = getColSpan(column, lastFrozenColumnIndex, {
      type: "HEADER"
    });
    if (colSpan !== void 0) {
      index += colSpan - 1;
    }
    cells.push((0, import_jsx_runtime.jsx)(HeaderCell, {
      column,
      colSpan,
      onResize: onColumnResize,
      allRowsSelected,
      onAllRowsSelectionChange: handleAllRowsSelectionChange,
      onSort,
      sortColumn,
      sortDirection
    }, column.key));
  }
  return (0, import_jsx_runtime.jsx)("div", {
    role: "row",
    "aria-rowindex": 1,
    className: headerRowClassname,
    children: cells
  });
}
var HeaderRow$1 = (0, import_react.memo)(HeaderRow);
function FilterRow({
  columns,
  filters,
  onFiltersChange
}) {
  function onChange(key, value) {
    const newFilters = {
      ...filters
    };
    newFilters[key] = value;
    onFiltersChange == null ? void 0 : onFiltersChange(newFilters);
  }
  return (0, import_jsx_runtime.jsx)("div", {
    role: "row",
    "aria-rowindex": 2,
    className: filterRowClassname,
    children: columns.map((column) => {
      const {
        key
      } = column;
      return (0, import_jsx_runtime.jsx)("div", {
        className: getCellClassname(column),
        style: getCellStyle(column),
        children: column.filterRenderer && (0, import_jsx_runtime.jsx)(column.filterRenderer, {
          column,
          value: filters == null ? void 0 : filters[column.key],
          onChange: (value) => onChange(key, value)
        })
      }, key);
    })
  });
}
var FilterRow$1 = (0, import_react.memo)(FilterRow);
var css_248z$3 = ".c1bmg16t700-canary43,.ccpfvsn700-canary43{background-color:#ccf}.c1bmg16t700-canary43.ccpfvsn700-canary43{background-color:#99f}.c12t67zz700-canary43{cursor:move;position:absolute;right:0;bottom:0;width:8px;height:8px;background-color:var(--selection-color)}.c12t67zz700-canary43:hover{width:16px;height:16px;border:2px solid var(--selection-color);background-color:var(--background-color)}";
styleInject(css_248z$3);
var cellCopied = "ccpfvsn700-canary43";
var cellCopiedClassname = `rdg-cell-copied ${cellCopied}`;
var cellDraggedOver = "c1bmg16t700-canary43";
var cellDraggedOverClassname = `rdg-cell-dragged-over ${cellDraggedOver}`;
var cellDragHandle = "c12t67zz700-canary43";
var cellDragHandleClassname = `rdg-cell-drag-handle ${cellDragHandle}`;
function Cell({
  className,
  column,
  colSpan,
  isCellSelected,
  isCopied,
  isDraggedOver,
  isRowSelected,
  row: row2,
  rowIdx,
  dragHandleProps,
  onRowClick,
  onClick,
  onDoubleClick,
  onContextMenu,
  onRowChange,
  selectCell,
  selectRow,
  ...props
}, ref) {
  const {
    cellClass
  } = column;
  className = getCellClassname(column, typeof cellClass === "function" ? cellClass(row2) : cellClass, className, isCellSelected && cellSelectedClassname, isCopied && cellCopiedClassname, isDraggedOver && cellDraggedOverClassname);
  function selectCellWrapper(openEditor) {
    selectCell({
      idx: column.idx,
      rowIdx
    }, openEditor);
  }
  function handleClick(event) {
    var _column$editorOptions;
    selectCellWrapper((_column$editorOptions = column.editorOptions) == null ? void 0 : _column$editorOptions.editOnClick);
    onRowClick == null ? void 0 : onRowClick(rowIdx, row2, column);
    onClick == null ? void 0 : onClick(event);
  }
  function handleContextMenu(event) {
    selectCellWrapper();
    onContextMenu == null ? void 0 : onContextMenu(event);
  }
  function handleDoubleClick(event) {
    selectCellWrapper(true);
    onDoubleClick == null ? void 0 : onDoubleClick(event);
  }
  function handleRowChange(newRow) {
    onRowChange(rowIdx, newRow);
  }
  function onRowSelectionChange(checked, isShiftClick) {
    selectRow({
      rowIdx,
      checked,
      isShiftClick
    });
  }
  return (0, import_jsx_runtime.jsx)("div", {
    role: "gridcell",
    "aria-colindex": column.idx + 1,
    "aria-selected": isCellSelected,
    "aria-colspan": colSpan,
    "aria-readonly": !isCellEditable(column, row2) || void 0,
    ref,
    className,
    style: getCellStyle(column, colSpan),
    onClick: handleClick,
    onDoubleClick: handleDoubleClick,
    onContextMenu: handleContextMenu,
    ...props,
    children: !column.rowGroup && (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, {
      children: [(0, import_jsx_runtime.jsx)(column.formatter, {
        column,
        rowIdx,
        row: row2,
        isCellSelected,
        isRowSelected,
        onRowSelectionChange,
        onRowChange: handleRowChange
      }), dragHandleProps && (0, import_jsx_runtime.jsx)("div", {
        className: cellDragHandleClassname,
        ...dragHandleProps
      })]
    })
  });
}
var Cell$1 = (0, import_react.memo)((0, import_react.forwardRef)(Cell));
var css_248z$2 = ".e1d24x2700-canary43{display:contents}";
styleInject(css_248z$2);
var editorContainer = "e1d24x2700-canary43";
var editorContainerClassname = `rdg-editor-container ${editorContainer}`;
function EditorContainer({
  row: row2,
  column,
  onRowChange,
  ...props
}) {
  var _column$editorOptions;
  const onClickCapture = useClickOutside(() => onRowChange(row2, true));
  if (column.editor === void 0) return null;
  const editor = (0, import_jsx_runtime.jsx)("div", {
    className: editorContainerClassname,
    onClickCapture,
    children: (0, import_jsx_runtime.jsx)(column.editor, {
      row: row2,
      column,
      onRowChange,
      ...props
    })
  });
  if ((_column$editorOptions = column.editorOptions) != null && _column$editorOptions.createPortal) {
    return (0, import_react_dom.createPortal)(editor, props.editorPortalTarget);
  }
  return editor;
}
var css_248z$1 = ".c1tngyp1700-canary43{padding:0}";
styleInject(css_248z$1);
var cellEditing = "c1tngyp1700-canary43";
var cellEditingClassname = `rdg-cell-editing ${cellEditing}`;
function EditCell({
  className,
  column,
  colSpan,
  row: row2,
  rowIdx,
  editorProps,
  ...props
}) {
  const [dimensions, setDimensions] = (0, import_react.useState)(null);
  const cellRef = (0, import_react.useCallback)((node) => {
    if (node !== null) {
      const {
        left,
        top
      } = node.getBoundingClientRect();
      setDimensions({
        left,
        top
      });
    }
  }, []);
  const {
    cellClass
  } = column;
  className = getCellClassname(column, cellSelectedClassname, cellEditingClassname, typeof cellClass === "function" ? cellClass(row2) : cellClass, className);
  function getCellContent() {
    var _document$scrollingEl;
    if (dimensions === null) return;
    const {
      scrollTop: docTop,
      scrollLeft: docLeft
    } = (_document$scrollingEl = document.scrollingElement) != null ? _document$scrollingEl : document.documentElement;
    const {
      left,
      top
    } = dimensions;
    const gridLeft = left + docLeft;
    const gridTop = top + docTop;
    return (0, import_jsx_runtime.jsx)(EditorContainer, {
      ...editorProps,
      rowIdx,
      column,
      left: gridLeft,
      top: gridTop
    });
  }
  return (0, import_jsx_runtime.jsx)("div", {
    role: "gridcell",
    "aria-colindex": column.idx + 1,
    "aria-selected": true,
    ref: cellRef,
    className,
    style: getCellStyle(column, colSpan),
    ...props,
    children: getCellContent()
  });
}
function Row({
  cellRenderer: CellRenderer = Cell$1,
  className,
  rowIdx,
  isRowSelected,
  copiedCellIdx,
  draggedOverCellIdx,
  lastFrozenColumnIndex,
  row: row2,
  viewportColumns,
  selectedCellProps,
  onRowClick,
  rowClass,
  setDraggedOverRowIdx,
  onMouseEnter,
  top,
  onRowChange,
  selectCell,
  selectRow,
  "aria-rowindex": ariaRowIndex,
  "aria-selected": ariaSelected,
  ...props
}, ref) {
  function handleDragEnter(event) {
    setDraggedOverRowIdx == null ? void 0 : setDraggedOverRowIdx(rowIdx);
    onMouseEnter == null ? void 0 : onMouseEnter(event);
  }
  className = clsx_m_default(rowClassname, `rdg-row-${rowIdx % 2 === 0 ? "even" : "odd"}`, rowClass == null ? void 0 : rowClass(row2), className, isRowSelected && rowSelectedClassname, (selectedCellProps == null ? void 0 : selectedCellProps.idx) === -1 && groupRowSelectedClassname);
  const cells = [];
  for (let index = 0; index < viewportColumns.length; index++) {
    const column = viewportColumns[index];
    const colSpan = getColSpan(column, lastFrozenColumnIndex, {
      type: "ROW",
      row: row2
    });
    if (colSpan !== void 0) {
      index += colSpan - 1;
    }
    const isCellSelected = (selectedCellProps == null ? void 0 : selectedCellProps.idx) === column.idx;
    if ((selectedCellProps == null ? void 0 : selectedCellProps.mode) === "EDIT" && isCellSelected) {
      cells.push((0, import_jsx_runtime.jsx)(EditCell, {
        rowIdx,
        column,
        colSpan,
        row: row2,
        onKeyDown: selectedCellProps.onKeyDown,
        editorProps: selectedCellProps.editorProps
      }, column.key));
      continue;
    }
    cells.push((0, import_jsx_runtime.jsx)(CellRenderer, {
      rowIdx,
      column,
      colSpan,
      row: row2,
      isCopied: copiedCellIdx === column.idx,
      isDraggedOver: draggedOverCellIdx === column.idx,
      isCellSelected,
      isRowSelected,
      dragHandleProps: isCellSelected ? selectedCellProps.dragHandleProps : void 0,
      onFocus: isCellSelected ? selectedCellProps.onFocus : void 0,
      onKeyDown: isCellSelected ? selectedCellProps.onKeyDown : void 0,
      onRowClick,
      onRowChange,
      selectCell,
      selectRow
    }, column.key));
  }
  return (0, import_jsx_runtime.jsx)("div", {
    role: "row",
    "aria-rowindex": ariaRowIndex,
    "aria-selected": ariaSelected,
    ref,
    className,
    onMouseEnter: handleDragEnter,
    style: {
      top
    },
    ...props,
    children: cells
  });
}
var Row$1 = (0, import_react.memo)((0, import_react.forwardRef)(Row));
function GroupCell({
  id,
  rowIdx,
  groupKey,
  childRows,
  isExpanded,
  isCellSelected,
  isRowSelected,
  column,
  groupColumnIndex,
  selectRow,
  toggleGroup: toggleGroupWrapper
}) {
  function toggleGroup() {
    toggleGroupWrapper(id);
  }
  function onRowSelectionChange(checked) {
    selectRow({
      rowIdx,
      checked,
      isShiftClick: false
    });
  }
  const isLevelMatching = column.rowGroup && groupColumnIndex === column.idx;
  return (0, import_jsx_runtime.jsx)("div", {
    role: "gridcell",
    "aria-colindex": column.idx + 1,
    className: getCellClassname(column, isCellSelected && cellSelectedClassname),
    style: {
      ...getCellStyle(column),
      cursor: isLevelMatching ? "pointer" : "default"
    },
    onClick: isLevelMatching ? toggleGroup : void 0,
    children: (!column.rowGroup || groupColumnIndex === column.idx) && column.groupFormatter && (0, import_jsx_runtime.jsx)(column.groupFormatter, {
      groupKey,
      childRows,
      column,
      isExpanded,
      isCellSelected,
      isRowSelected,
      onRowSelectionChange,
      toggleGroup
    })
  }, column.key);
}
var GroupCell$1 = (0, import_react.memo)(GroupCell);
function GroupedRow({
  id,
  groupKey,
  viewportColumns,
  childRows,
  rowIdx,
  top,
  level,
  isExpanded,
  selectedCellIdx,
  isRowSelected,
  selectCell,
  selectRow,
  toggleGroup,
  ...props
}) {
  const idx = viewportColumns[0].key === SELECT_COLUMN_KEY ? level + 1 : level;
  function selectGroup() {
    selectCell({
      rowIdx,
      idx: -1
    });
  }
  return (0, import_jsx_runtime.jsx)("div", {
    role: "row",
    "aria-level": level,
    "aria-expanded": isExpanded,
    className: clsx_m_default(rowClassname, groupRowClassname, `rdg-row-${rowIdx % 2 === 0 ? "even" : "odd"}`, isRowSelected && rowSelectedClassname, selectedCellIdx === -1 && groupRowSelectedClassname),
    onClick: selectGroup,
    style: {
      top
    },
    ...props,
    children: viewportColumns.map((column) => (0, import_jsx_runtime.jsx)(GroupCell$1, {
      id,
      rowIdx,
      groupKey,
      childRows,
      isExpanded,
      isRowSelected,
      isCellSelected: selectedCellIdx === column.idx,
      column,
      groupColumnIndex: idx,
      selectRow,
      toggleGroup
    }, column.key))
  });
}
var GroupRowRenderer = (0, import_react.memo)(GroupedRow);
function SummaryCell({
  column,
  colSpan,
  row: row2
}) {
  const {
    summaryFormatter: SummaryFormatter,
    summaryCellClass
  } = column;
  const className = getCellClassname(column, typeof summaryCellClass === "function" ? summaryCellClass(row2) : summaryCellClass);
  return (0, import_jsx_runtime.jsx)("div", {
    role: "gridcell",
    "aria-colindex": column.idx + 1,
    "aria-colspan": colSpan,
    className,
    style: getCellStyle(column, colSpan),
    children: SummaryFormatter && (0, import_jsx_runtime.jsx)(SummaryFormatter, {
      column,
      row: row2
    })
  });
}
var SummaryCell$1 = (0, import_react.memo)(SummaryCell);
function SummaryRow({
  rowIdx,
  row: row2,
  viewportColumns,
  bottom,
  lastFrozenColumnIndex,
  "aria-rowindex": ariaRowIndex
}) {
  const cells = [];
  for (let index = 0; index < viewportColumns.length; index++) {
    const column = viewportColumns[index];
    const colSpan = getColSpan(column, lastFrozenColumnIndex, {
      type: "SUMMARY",
      row: row2
    });
    if (colSpan !== void 0) {
      index += colSpan - 1;
    }
    cells.push((0, import_jsx_runtime.jsx)(SummaryCell$1, {
      column,
      colSpan,
      row: row2
    }, column.key));
  }
  return (0, import_jsx_runtime.jsx)("div", {
    role: "row",
    "aria-rowindex": ariaRowIndex,
    className: `${rowClassname} rdg-row-${rowIdx % 2 === 0 ? "even" : "odd"} ${summaryRowClassname}`,
    style: {
      bottom
    },
    children: cells
  });
}
var SummaryRow$1 = (0, import_react.memo)(SummaryRow);
var _globalThis$document;
var body = (_globalThis$document = globalThis.document) == null ? void 0 : _globalThis$document.body;
function DataGrid({
  columns: rawColumns,
  rows: rawRows,
  summaryRows,
  rowKeyGetter,
  onRowsChange,
  rowHeight = 35,
  headerRowHeight = rowHeight,
  headerFiltersHeight = 45,
  summaryRowHeight = rowHeight,
  selectedRows,
  onSelectedRowsChange,
  sortColumn,
  sortDirection,
  onSort,
  filters,
  onFiltersChange,
  defaultColumnOptions,
  groupBy: rawGroupBy,
  rowGrouper,
  expandedGroupIds,
  onExpandedGroupIdsChange,
  rowRenderer: RowRenderer = Row$1,
  emptyRowsRenderer: EmptyRowsRenderer,
  onRowClick,
  onScroll,
  onColumnResize,
  onSelectedCellChange,
  onFill,
  onPaste,
  enableFilterRow = false,
  cellNavigationMode = "NONE",
  enableVirtualization = true,
  editorPortalTarget = body,
  className,
  style,
  rowClass,
  "aria-label": ariaLabel,
  "aria-labelledby": ariaLabelledBy,
  "aria-describedby": ariaDescribedBy
}, ref) {
  var _summaryRows$length;
  const [scrollTop, setScrollTop] = (0, import_react.useState)(0);
  const [scrollLeft, setScrollLeft] = (0, import_react.useState)(0);
  const [columnWidths, setColumnWidths] = (0, import_react.useState)(() => /* @__PURE__ */ new Map());
  const [selectedPosition, setSelectedPosition] = (0, import_react.useState)({
    idx: -1,
    rowIdx: -1,
    mode: "SELECT"
  });
  const [copiedCell, setCopiedCell] = (0, import_react.useState)(null);
  const [isDragging, setDragging] = (0, import_react.useState)(false);
  const [draggedOverRowIdx, setOverRowIdx] = (0, import_react.useState)(void 0);
  const focusSinkRef = (0, import_react.useRef)(null);
  const prevSelectedPosition = (0, import_react.useRef)(selectedPosition);
  const latestDraggedOverRowIdx = (0, import_react.useRef)(draggedOverRowIdx);
  const lastSelectedRowIdx = (0, import_react.useRef)(-1);
  const isCellFocusable = (0, import_react.useRef)(false);
  const selectRowWrapper = useLatestFunc(selectRow);
  const selectCellWrapper = useLatestFunc(selectCell);
  const toggleGroupWrapper = useLatestFunc(toggleGroup);
  const handleFormatterRowChangeWrapper = useLatestFunc(updateRow);
  const [gridRef, gridWidth, gridHeight] = useGridDimensions();
  const headerRowsCount = enableFilterRow ? 2 : 1;
  const summaryRowsCount = (_summaryRows$length = summaryRows == null ? void 0 : summaryRows.length) != null ? _summaryRows$length : 0;
  const totalHeaderHeight = headerRowHeight + (enableFilterRow ? headerFiltersHeight : 0);
  const clientHeight = gridHeight - totalHeaderHeight - summaryRowsCount * summaryRowHeight;
  const isSelectable = selectedRows !== void 0 && onSelectedRowsChange !== void 0;
  const {
    columns,
    colSpanColumns,
    colOverscanStartIdx,
    colOverscanEndIdx,
    layoutCssVars,
    columnMetrics,
    totalColumnWidth,
    lastFrozenColumnIndex,
    totalFrozenColumnWidth,
    groupBy
  } = useCalculatedColumns({
    rawColumns,
    columnWidths,
    scrollLeft,
    viewportWidth: gridWidth,
    defaultColumnOptions,
    rawGroupBy: rowGrouper ? rawGroupBy : void 0,
    enableVirtualization
  });
  const {
    rowOverscanStartIdx,
    rowOverscanEndIdx,
    rows,
    rowsCount,
    isGroupRow
  } = useViewportRows({
    rawRows,
    groupBy,
    rowGrouper,
    rowHeight,
    clientHeight,
    scrollTop,
    expandedGroupIds,
    enableVirtualization
  });
  const viewportColumns = useViewportColumns({
    columns,
    colSpanColumns,
    colOverscanStartIdx,
    colOverscanEndIdx,
    lastFrozenColumnIndex,
    rowOverscanStartIdx,
    rowOverscanEndIdx,
    rows,
    summaryRows,
    enableFilterRow,
    isGroupRow
  });
  const hasGroups = groupBy.length > 0 && typeof rowGrouper === "function";
  const minColIdx = hasGroups ? -1 : 0;
  const enableCellDragAndDrop = hasGroups ? false : onFill !== void 0;
  (0, import_react.useLayoutEffect)(() => {
    if (selectedPosition === prevSelectedPosition.current || selectedPosition.mode === "EDIT" || !isCellWithinBounds(selectedPosition)) return;
    prevSelectedPosition.current = selectedPosition;
    scrollToCell(selectedPosition);
    if (isCellFocusable.current) {
      isCellFocusable.current = false;
      return;
    }
    focusSinkRef.current.focus({
      preventScroll: true
    });
  });
  (0, import_react.useImperativeHandle)(ref, () => ({
    element: gridRef.current,
    scrollToColumn(idx) {
      scrollToCell({
        idx
      });
    },
    scrollToRow(rowIdx) {
      const {
        current
      } = gridRef;
      if (!current) return;
      current.scrollTo({
        top: rowIdx * rowHeight,
        behavior: "smooth"
      });
    },
    selectCell
  }));
  const handleColumnResize = (0, import_react.useCallback)((column, width) => {
    setColumnWidths((columnWidths2) => {
      const newColumnWidths = new Map(columnWidths2);
      newColumnWidths.set(column.key, width);
      return newColumnWidths;
    });
    onColumnResize == null ? void 0 : onColumnResize(column.idx, width);
  }, [onColumnResize]);
  const setDraggedOverRowIdx = (0, import_react.useCallback)((rowIdx) => {
    setOverRowIdx(rowIdx);
    latestDraggedOverRowIdx.current = rowIdx;
  }, []);
  function selectRow({
    rowIdx,
    checked,
    isShiftClick
  }) {
    if (!onSelectedRowsChange) return;
    assertIsValidKeyGetter(rowKeyGetter);
    const newSelectedRows = new Set(selectedRows);
    const row2 = rows[rowIdx];
    if (isGroupRow(row2)) {
      for (const childRow of row2.childRows) {
        const rowKey2 = rowKeyGetter(childRow);
        if (checked) {
          newSelectedRows.add(rowKey2);
        } else {
          newSelectedRows.delete(rowKey2);
        }
      }
      onSelectedRowsChange(newSelectedRows);
      return;
    }
    const rowKey = rowKeyGetter(row2);
    if (checked) {
      newSelectedRows.add(rowKey);
      const previousRowIdx = lastSelectedRowIdx.current;
      lastSelectedRowIdx.current = rowIdx;
      if (isShiftClick && previousRowIdx !== -1 && previousRowIdx !== rowIdx) {
        const step = Math.sign(rowIdx - previousRowIdx);
        for (let i = previousRowIdx + step; i !== rowIdx; i += step) {
          const row3 = rows[i];
          if (isGroupRow(row3)) continue;
          newSelectedRows.add(rowKeyGetter(row3));
        }
      }
    } else {
      newSelectedRows.delete(rowKey);
      lastSelectedRowIdx.current = -1;
    }
    onSelectedRowsChange(newSelectedRows);
  }
  function toggleGroup(expandedGroupId) {
    if (!onExpandedGroupIdsChange) return;
    const newExpandedGroupIds = new Set(expandedGroupIds);
    if (newExpandedGroupIds.has(expandedGroupId)) {
      newExpandedGroupIds.delete(expandedGroupId);
    } else {
      newExpandedGroupIds.add(expandedGroupId);
    }
    onExpandedGroupIdsChange(newExpandedGroupIds);
  }
  function onGridFocus() {
    if (!isCellWithinBounds(selectedPosition)) {
      const initialPosition = {
        idx: 0,
        rowIdx: 0,
        mode: "SELECT"
      };
      if (isCellWithinBounds(initialPosition)) {
        setSelectedPosition(initialPosition);
      }
    } else {
      scrollToCell(selectedPosition);
    }
  }
  function handleKeyDown(event) {
    const {
      key,
      keyCode
    } = event;
    const row2 = rows[selectedPosition.rowIdx];
    if (onPaste && isCtrlKeyHeldDown(event) && isCellWithinBounds(selectedPosition) && !isGroupRow(row2) && selectedPosition.idx !== -1 && selectedPosition.mode === "SELECT") {
      const cKey = 67;
      const vKey = 86;
      if (keyCode === cKey) {
        handleCopy();
        return;
      }
      if (keyCode === vKey) {
        handlePaste();
        return;
      }
    }
    if (isCellWithinBounds(selectedPosition) && isGroupRow(row2) && selectedPosition.idx === -1 && (key === "ArrowLeft" && row2.isExpanded || key === "ArrowRight" && !row2.isExpanded)) {
      event.preventDefault();
      toggleGroup(row2.id);
      return;
    }
    switch (event.key) {
      case "Escape":
        setCopiedCell(null);
        closeEditor();
        return;
      case "ArrowUp":
      case "ArrowDown":
      case "ArrowLeft":
      case "ArrowRight":
      case "Tab":
      case "Home":
      case "End":
      case "PageUp":
      case "PageDown":
        navigate(event);
        break;
      default:
        handleCellInput(event);
        break;
    }
  }
  function handleFocus() {
    isCellFocusable.current = true;
  }
  function handleScroll(event) {
    const {
      scrollTop: scrollTop2,
      scrollLeft: scrollLeft2
    } = event.currentTarget;
    setScrollTop(scrollTop2);
    setScrollLeft(scrollLeft2);
    onScroll == null ? void 0 : onScroll(event);
  }
  function getRawRowIdx(rowIdx) {
    return hasGroups ? rawRows.indexOf(rows[rowIdx]) : rowIdx;
  }
  function updateRow(rowIdx, row2) {
    if (typeof onRowsChange !== "function") return;
    const updatedRows = [...rawRows];
    updatedRows[rowIdx] = row2;
    onRowsChange(updatedRows, {
      indexes: [rowIdx],
      column: columns[selectedPosition.idx]
    });
  }
  function commitEditorChanges() {
    var _columns$selectedPosi;
    if (((_columns$selectedPosi = columns[selectedPosition.idx]) == null ? void 0 : _columns$selectedPosi.editor) === void 0 || selectedPosition.mode === "SELECT" || selectedPosition.row === selectedPosition.originalRow) {
      return;
    }
    const rowIdx = getRawRowIdx(selectedPosition.rowIdx);
    updateRow(rowIdx, selectedPosition.row);
  }
  function handleCopy() {
    const {
      idx,
      rowIdx
    } = selectedPosition;
    setCopiedCell({
      row: rawRows[getRawRowIdx(rowIdx)],
      columnKey: columns[idx].key
    });
  }
  function handlePaste() {
    const {
      idx,
      rowIdx
    } = selectedPosition;
    const targetRow = rawRows[getRawRowIdx(rowIdx)];
    if (!onPaste || !onRowsChange || copiedCell === null || !isCellEditable2(selectedPosition)) {
      return;
    }
    const updatedTargetRow = onPaste({
      sourceRow: copiedCell.row,
      sourceColumnKey: copiedCell.columnKey,
      targetRow,
      targetColumnKey: columns[idx].key
    });
    updateRow(rowIdx, updatedTargetRow);
  }
  function handleCellInput(event) {
    var _column$editorOptions;
    if (!isCellWithinBounds(selectedPosition)) return;
    const row2 = rows[selectedPosition.rowIdx];
    if (isGroupRow(row2)) return;
    const {
      key
    } = event;
    const column = columns[selectedPosition.idx];
    if (selectedPosition.mode === "EDIT") {
      if (key === "Enter") {
        commitEditorChanges();
        closeEditor();
      }
      return;
    }
    (_column$editorOptions = column.editorOptions) == null ? void 0 : _column$editorOptions.onCellKeyDown == null ? void 0 : _column$editorOptions.onCellKeyDown(event);
    if (event.isDefaultPrevented()) return;
    if (isCellEditable2(selectedPosition) && isDefaultCellInput(event)) {
      setSelectedPosition(({
        idx,
        rowIdx
      }) => ({
        idx,
        rowIdx,
        key,
        mode: "EDIT",
        row: row2,
        originalRow: row2
      }));
    }
  }
  function handleDragEnd() {
    const overRowIdx = latestDraggedOverRowIdx.current;
    if (overRowIdx === void 0 || !onFill || !onRowsChange) return;
    const {
      idx,
      rowIdx
    } = selectedPosition;
    const sourceRow = rawRows[rowIdx];
    const startRowIndex = rowIdx < overRowIdx ? rowIdx + 1 : overRowIdx;
    const endRowIndex = rowIdx < overRowIdx ? overRowIdx + 1 : rowIdx;
    const targetRows = rawRows.slice(startRowIndex, endRowIndex);
    const column = columns[idx];
    const updatedTargetRows = onFill({
      columnKey: column.key,
      sourceRow,
      targetRows
    });
    const updatedRows = [...rawRows];
    const indexes = [];
    for (let i = startRowIndex; i < endRowIndex; i++) {
      updatedRows[i] = updatedTargetRows[i - startRowIndex];
      indexes.push(i);
    }
    onRowsChange(updatedRows, {
      indexes,
      column
    });
    setDraggedOverRowIdx(void 0);
  }
  function handleMouseDown(event) {
    if (event.buttons !== 1) return;
    setDragging(true);
    window.addEventListener("mouseover", onMouseOver);
    window.addEventListener("mouseup", onMouseUp);
    function onMouseOver(event2) {
      if (event2.buttons !== 1) onMouseUp();
    }
    function onMouseUp() {
      window.removeEventListener("mouseover", onMouseOver);
      window.removeEventListener("mouseup", onMouseUp);
      setDragging(false);
      handleDragEnd();
    }
  }
  function handleDoubleClick(event) {
    event.stopPropagation();
    if (!onFill || !onRowsChange) return;
    const {
      idx,
      rowIdx
    } = selectedPosition;
    const sourceRow = rawRows[rowIdx];
    const targetRows = rawRows.slice(rowIdx + 1);
    const column = columns[idx];
    const updatedTargetRows = onFill({
      columnKey: column.key,
      sourceRow,
      targetRows
    });
    const updatedRows = [...rawRows];
    const indexes = [];
    for (let i = rowIdx + 1; i < updatedRows.length; i++) {
      updatedRows[i] = updatedTargetRows[i - rowIdx - 1];
      indexes.push(i);
    }
    onRowsChange(updatedRows, {
      indexes,
      column
    });
  }
  function handleEditorRowChange(row2, commitChanges) {
    if (selectedPosition.mode === "SELECT") return;
    if (commitChanges) {
      updateRow(getRawRowIdx(selectedPosition.rowIdx), row2);
      closeEditor();
    } else {
      setSelectedPosition((position) => ({
        ...position,
        row: row2
      }));
    }
  }
  function handleOnClose(commitChanges) {
    if (commitChanges) {
      commitEditorChanges();
    }
    closeEditor();
  }
  function isCellWithinBounds({
    idx,
    rowIdx
  }) {
    return rowIdx >= 0 && rowIdx < rows.length && idx >= minColIdx && idx < columns.length;
  }
  function isCellEditable2(position) {
    return isCellWithinBounds(position) && isSelectedCellEditable({
      columns,
      rows,
      selectedPosition: position,
      isGroupRow
    });
  }
  function selectCell(position, enableEditor = false) {
    if (!isCellWithinBounds(position)) return;
    commitEditorChanges();
    if (enableEditor && isCellEditable2(position)) {
      const row2 = rows[position.rowIdx];
      setSelectedPosition({
        ...position,
        mode: "EDIT",
        key: null,
        row: row2,
        originalRow: row2
      });
    } else {
      setSelectedPosition({
        ...position,
        mode: "SELECT"
      });
    }
    onSelectedCellChange == null ? void 0 : onSelectedCellChange({
      ...position
    });
  }
  function closeEditor() {
    if (selectedPosition.mode === "SELECT") return;
    setSelectedPosition(({
      idx,
      rowIdx
    }) => ({
      idx,
      rowIdx,
      mode: "SELECT"
    }));
  }
  function scrollToCell({
    idx,
    rowIdx
  }) {
    const {
      current
    } = gridRef;
    if (!current) return;
    if (typeof idx === "number" && idx > lastFrozenColumnIndex) {
      var _rowIdx;
      (_rowIdx = rowIdx) != null ? _rowIdx : rowIdx = selectedPosition.rowIdx;
      if (!isCellWithinBounds({
        rowIdx,
        idx
      })) return;
      const {
        clientWidth
      } = current;
      const column = columns[idx];
      const {
        left,
        width
      } = columnMetrics.get(column);
      let right = left + width;
      const row2 = rows[rowIdx];
      if (!isGroupRow(row2)) {
        const colSpan = getColSpan(column, lastFrozenColumnIndex, {
          type: "ROW",
          row: row2
        });
        if (colSpan !== void 0) {
          const {
            left: left2,
            width: width2
          } = columnMetrics.get(columns[column.idx + colSpan - 1]);
          right = left2 + width2;
        }
      }
      const isCellAtLeftBoundary = left < scrollLeft + totalFrozenColumnWidth;
      const isCellAtRightBoundary = right > clientWidth + scrollLeft;
      if (isCellAtLeftBoundary) {
        current.scrollLeft = left - totalFrozenColumnWidth;
      } else if (isCellAtRightBoundary) {
        current.scrollLeft = right - clientWidth;
      }
    }
    if (typeof rowIdx === "number") {
      if (rowIdx * rowHeight < scrollTop) {
        current.scrollTop = rowIdx * rowHeight;
      } else if ((rowIdx + 1) * rowHeight > scrollTop + clientHeight) {
        current.scrollTop = (rowIdx + 1) * rowHeight - clientHeight;
      }
    }
  }
  function getNextPosition(key, ctrlKey, shiftKey) {
    const {
      idx,
      rowIdx
    } = selectedPosition;
    const row2 = rows[rowIdx];
    const isRowSelected = isCellWithinBounds(selectedPosition) && idx === -1;
    if (key === "ArrowLeft" && isRowSelected && isGroupRow(row2) && !row2.isExpanded && row2.level !== 0) {
      let parentRowIdx = -1;
      for (let i = selectedPosition.rowIdx - 1; i >= 0; i--) {
        const parentRow = rows[i];
        if (isGroupRow(parentRow) && parentRow.id === row2.parentId) {
          parentRowIdx = i;
          break;
        }
      }
      if (parentRowIdx !== -1) {
        return {
          idx,
          rowIdx: parentRowIdx
        };
      }
    }
    switch (key) {
      case "ArrowUp":
        return {
          idx,
          rowIdx: rowIdx - 1
        };
      case "ArrowDown":
        return {
          idx,
          rowIdx: rowIdx + 1
        };
      case "ArrowLeft":
        return {
          idx: idx - 1,
          rowIdx
        };
      case "ArrowRight":
        return {
          idx: idx + 1,
          rowIdx
        };
      case "Tab":
        if (selectedPosition.idx === -1 && selectedPosition.rowIdx === -1) {
          return shiftKey ? {
            idx: columns.length - 1,
            rowIdx: rows.length - 1
          } : {
            idx: 0,
            rowIdx: 0
          };
        }
        return {
          idx: idx + (shiftKey ? -1 : 1),
          rowIdx
        };
      case "Home":
        if (isRowSelected) return {
          idx,
          rowIdx: 0
        };
        return ctrlKey ? {
          idx: 0,
          rowIdx: 0
        } : {
          idx: 0,
          rowIdx
        };
      case "End":
        if (isRowSelected) return {
          idx,
          rowIdx: rows.length - 1
        };
        return ctrlKey ? {
          idx: columns.length - 1,
          rowIdx: rows.length - 1
        } : {
          idx: columns.length - 1,
          rowIdx
        };
      case "PageUp":
        return {
          idx,
          rowIdx: rowIdx - Math.floor(clientHeight / rowHeight)
        };
      case "PageDown":
        return {
          idx,
          rowIdx: rowIdx + Math.floor(clientHeight / rowHeight)
        };
      default:
        return selectedPosition;
    }
  }
  function navigate(event) {
    if (selectedPosition.mode === "EDIT") {
      var _columns$selectedPosi2, _columns$selectedPosi3;
      const onNavigation = (_columns$selectedPosi2 = (_columns$selectedPosi3 = columns[selectedPosition.idx].editorOptions) == null ? void 0 : _columns$selectedPosi3.onNavigation) != null ? _columns$selectedPosi2 : onEditorNavigation;
      if (!onNavigation(event)) return;
    }
    const {
      key,
      shiftKey
    } = event;
    let mode = cellNavigationMode;
    if (key === "Tab") {
      if (canExitGrid({
        shiftKey,
        cellNavigationMode,
        columns,
        rowsCount: rows.length,
        selectedPosition
      })) {
        commitEditorChanges();
        return;
      }
      mode = cellNavigationMode === "NONE" ? "CHANGE_ROW" : cellNavigationMode;
    }
    event.preventDefault();
    const ctrlKey = isCtrlKeyHeldDown(event);
    const nextPosition = getNextSelectedCellPosition({
      columns,
      colSpanColumns,
      rows,
      lastFrozenColumnIndex,
      cellNavigationMode: mode,
      currentPosition: selectedPosition,
      nextPosition: getNextPosition(key, ctrlKey, shiftKey),
      isCellWithinBounds,
      isGroupRow
    });
    selectCell(nextPosition);
  }
  function getDraggedOverCellIdx(currentRowIdx) {
    if (draggedOverRowIdx === void 0) return;
    const {
      rowIdx
    } = selectedPosition;
    const isDraggedOver = rowIdx < draggedOverRowIdx ? rowIdx < currentRowIdx && currentRowIdx <= draggedOverRowIdx : rowIdx > currentRowIdx && currentRowIdx >= draggedOverRowIdx;
    return isDraggedOver ? selectedPosition.idx : void 0;
  }
  function getSelectedCellProps(rowIdx) {
    if (selectedPosition.rowIdx !== rowIdx) return;
    if (selectedPosition.mode === "EDIT") {
      return {
        mode: "EDIT",
        idx: selectedPosition.idx,
        onKeyDown: handleKeyDown,
        editorProps: {
          editorPortalTarget,
          rowHeight,
          row: selectedPosition.row,
          onRowChange: handleEditorRowChange,
          onClose: handleOnClose
        }
      };
    }
    return {
      mode: "SELECT",
      idx: selectedPosition.idx,
      onFocus: handleFocus,
      onKeyDown: handleKeyDown,
      dragHandleProps: enableCellDragAndDrop && isCellEditable2(selectedPosition) ? {
        onMouseDown: handleMouseDown,
        onDoubleClick: handleDoubleClick
      } : void 0
    };
  }
  function getViewportRows() {
    const rowElements = [];
    let startRowIndex = 0;
    for (let rowIdx = rowOverscanStartIdx; rowIdx <= rowOverscanEndIdx; rowIdx++) {
      const row2 = rows[rowIdx];
      const top = rowIdx * rowHeight + totalHeaderHeight;
      if (isGroupRow(row2)) {
        ({
          startRowIndex
        } = row2);
        rowElements.push((0, import_jsx_runtime.jsx)(GroupRowRenderer, {
          "aria-level": row2.level + 1,
          "aria-setsize": row2.setSize,
          "aria-posinset": row2.posInSet + 1,
          "aria-rowindex": headerRowsCount + startRowIndex + 1,
          id: row2.id,
          groupKey: row2.groupKey,
          viewportColumns,
          childRows: row2.childRows,
          rowIdx,
          top,
          level: row2.level,
          isExpanded: row2.isExpanded,
          selectedCellIdx: selectedPosition.rowIdx === rowIdx ? selectedPosition.idx : void 0,
          isRowSelected: isSelectable && row2.childRows.every((cr) => selectedRows == null ? void 0 : selectedRows.has(rowKeyGetter(cr))),
          onFocus: selectedPosition.rowIdx === rowIdx ? handleFocus : void 0,
          onKeyDown: selectedPosition.rowIdx === rowIdx ? handleKeyDown : void 0,
          selectCell: selectCellWrapper,
          selectRow: selectRowWrapper,
          toggleGroup: toggleGroupWrapper
        }, row2.id));
        continue;
      }
      startRowIndex++;
      let key = hasGroups ? startRowIndex : rowIdx;
      let isRowSelected = false;
      if (typeof rowKeyGetter === "function") {
        var _selectedRows$has;
        key = rowKeyGetter(row2);
        isRowSelected = (_selectedRows$has = selectedRows == null ? void 0 : selectedRows.has(key)) != null ? _selectedRows$has : false;
      }
      rowElements.push((0, import_jsx_runtime.jsx)(RowRenderer, {
        "aria-rowindex": headerRowsCount + (hasGroups ? startRowIndex : rowIdx) + 1,
        "aria-selected": isSelectable ? isRowSelected : void 0,
        rowIdx,
        row: row2,
        viewportColumns,
        isRowSelected,
        onRowClick,
        rowClass,
        top,
        copiedCellIdx: copiedCell !== null && copiedCell.row === row2 ? columns.findIndex((c) => c.key === copiedCell.columnKey) : void 0,
        draggedOverCellIdx: getDraggedOverCellIdx(rowIdx),
        setDraggedOverRowIdx: isDragging ? setDraggedOverRowIdx : void 0,
        lastFrozenColumnIndex,
        selectedCellProps: getSelectedCellProps(rowIdx),
        onRowChange: handleFormatterRowChangeWrapper,
        selectCell: selectCellWrapper,
        selectRow: selectRowWrapper
      }, key));
    }
    return rowElements;
  }
  if (selectedPosition.idx >= columns.length || selectedPosition.rowIdx >= rows.length) {
    setSelectedPosition({
      idx: -1,
      rowIdx: -1,
      mode: "SELECT"
    });
    setDraggedOverRowIdx(void 0);
  }
  if (selectedPosition.mode === "EDIT" && rows[selectedPosition.rowIdx] !== selectedPosition.originalRow) {
    closeEditor();
  }
  return (0, import_jsx_runtime.jsxs)("div", {
    role: hasGroups ? "treegrid" : "grid",
    "aria-label": ariaLabel,
    "aria-labelledby": ariaLabelledBy,
    "aria-describedby": ariaDescribedBy,
    "aria-multiselectable": isSelectable ? true : void 0,
    "aria-colcount": columns.length,
    "aria-rowcount": headerRowsCount + rowsCount + summaryRowsCount,
    className: clsx_m_default(rootClassname, className, isDragging && viewportDraggingClassname),
    style: {
      ...style,
      "--header-row-height": `${headerRowHeight}px`,
      "--filter-row-height": `${headerFiltersHeight}px`,
      "--row-width": `${totalColumnWidth}px`,
      "--row-height": `${rowHeight}px`,
      "--summary-row-height": `${summaryRowHeight}px`,
      ...layoutCssVars
    },
    ref: gridRef,
    onScroll: handleScroll,
    children: [(0, import_jsx_runtime.jsx)(HeaderRow$1, {
      rowKeyGetter,
      rows: rawRows,
      columns: viewportColumns,
      onColumnResize: handleColumnResize,
      allRowsSelected: (selectedRows == null ? void 0 : selectedRows.size) === rawRows.length,
      onSelectedRowsChange,
      sortColumn,
      sortDirection,
      onSort,
      lastFrozenColumnIndex
    }), enableFilterRow && (0, import_jsx_runtime.jsx)(FilterRow$1, {
      columns: viewportColumns,
      filters,
      onFiltersChange
    }), rows.length === 0 && EmptyRowsRenderer ? (0, import_jsx_runtime.jsx)(EmptyRowsRenderer, {}) : (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, {
      children: [(0, import_jsx_runtime.jsx)("div", {
        ref: focusSinkRef,
        tabIndex: 0,
        className: focusSinkClassname,
        onKeyDown: handleKeyDown,
        onFocus: onGridFocus
      }), (0, import_jsx_runtime.jsx)("div", {
        style: {
          height: Math.max(rows.length * rowHeight, clientHeight)
        }
      }), getViewportRows(), summaryRows == null ? void 0 : summaryRows.map((row2, rowIdx) => (0, import_jsx_runtime.jsx)(SummaryRow$1, {
        "aria-rowindex": headerRowsCount + rowsCount + rowIdx + 1,
        rowIdx,
        row: row2,
        bottom: summaryRowHeight * (summaryRows.length - 1 - rowIdx),
        viewportColumns,
        lastFrozenColumnIndex
      }, rowIdx))]
    })]
  });
}
var DataGrid$1 = (0, import_react.forwardRef)(DataGrid);
var css_248z = ".t16y9g8l700-canary43{appearance:none;box-sizing:border-box;width:100%;height:100%;padding:0 6px;border:2px solid #ccc;vertical-align:top;color:var(--color);background-color:var(--background-color);font-family:inherit;font-size:var(--font-size)}.t16y9g8l700-canary43:focus{border-color:var(--selection-color);outline:none}.t16y9g8l700-canary43::placeholder{color:#999;opacity:1}";
styleInject(css_248z);
var textEditor = "t16y9g8l700-canary43";
var textEditorClassname = `rdg-text-editor ${textEditor}`;
function autoFocusAndSelect(input) {
  input == null ? void 0 : input.focus();
  input == null ? void 0 : input.select();
}
function TextEditor({
  row: row2,
  column,
  onRowChange,
  onClose
}) {
  return (0, import_jsx_runtime.jsx)("input", {
    className: textEditorClassname,
    ref: autoFocusAndSelect,
    value: row2[column.key],
    onChange: (event) => onRowChange({
      ...row2,
      [column.key]: event.target.value
    }),
    onBlur: () => onClose(true)
  });
}
var bundle_default = DataGrid$1;
export {
  Cell$1 as Cell,
  Row$1 as Row,
  SELECT_COLUMN_KEY,
  SelectCellFormatter,
  SelectColumn,
  SortableHeaderCell,
  TextEditor,
  ToggleGroupFormatter,
  ValueFormatter,
  bundle_default as default
};
//# sourceMappingURL=react-data-grid.js.map
